export enum Targets {
	self,
	mouse_enemy,
	mouse_proj,
	deck,
}

export enum Symbols {
	sword,
	plus,
	sheild,
	potion,
	flame,
	waves,
	flake,
}

export enum Stats {
	health,
	attack,
	power,
	speed,
	duration,
}

export enum Colors {
	red,
	blue,
	green,
	white,
	purple,
}

export enum States {
	world,
	deck,
	hand,
	mouse,
	self,
	enemy,
}

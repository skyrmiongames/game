export enum Targets {
    self,
    self_area,
    mouse_enemy,
    mouse_proj,
    mouse_cone,
    mouse_area,
    deck,
}

export enum Symbols {
    sword,
    plus,
    sheild,
    potion,
    flame,
    waves,
    flake,
}

export enum Stats {
    health,
    attack,
    power,
    range,
    speed,
    duration,
}

export enum Colors {
    red,
    blue,
    green,
    white,
    purple,
}
